# Issue 252 distribution repair contract

Domain obligation: installing advisory skill text must not grant Aegis authority, overwrite local edits, alter normal profiles during verification, or label packaging as agent behavior. The explicit destination owns only its official `skills` inventory; runtime credentials, config, and other profile state remain outside the transaction.

Concrete exception for additional candidate-lineage repair: F8 remains reproducible in `internal/skillbundle/cmd/main.go`: only validate/evaluate/build/verify actions exist, while docs require exact-artifact inventory, update and rollback. No executable transaction currently implements those obligations. Cumulative prior turns are unknown, not zero. This repair invalidates distribution/packaging and corresponding documentation proof; it does not resolve F1, F2/F3/F6, F4, F5, F7 or F9.

Technical contract: add a bounded POSIX managed-inventory transaction with exact archive SHA-256 and Git revision, private verified generations, an atomically replaced skills pointer, exclusive process lock, exact installed readback, and no adoption of an existing unmanaged skills directory. Retained generations allow an explicitly named verified rollback through the same transaction. Refuse unsafe paths, incompatible layouts and any local inventory modification. Interruption before pointer replacement retains the old inventory; after replacement retains the new inventory. No journal may grant authority. A process interruption must release the operating-system lock. No implicit normal-profile destination, downloads, provider changes, session activation, second store writer or credential intake.

Acceptance checks: clean install of all manifest skills/dependencies; source-independent exact inventory; exact source/digest mismatch denial; no unmanaged adoption; local-edit refusal; upgrade and rollback; concurrent exclusion; interrupted pointer publication; cancellation; explicit platform denial where atomic-pointer/lock semantics are not implemented. These are distribution tests, not Hermes behavioral or supported-version qualification.

Current source reconciliation: HEAD 7fe043cfce6c4afb178c35a9e857f12e8b64bbfe is the documented accepted replay repair atop packet candidate 9db84a9c0ab0609b79b2ebc02448b2a38817009b. Canonical session still identifies attempt 5 and packet tflp_b22c2a8d2d1545c5 / 7cc03027a9220797ca7f7cd325364a21a948820557344660f7d29bc9e10976b4. GitHub #248 is open; #245 is closed, and local origin/main includes its #254 delivery. This readback is not interface integration proof. Both preexisting untracked artifacts remain untouched.

## Observed implementation and verification

The managed installer and exact inventory readback are implemented, including retained generations, process locking, atomic pointer publication, explicit update/rollback, local-edit and unmanaged-directory refusal, cancellation, and bounded archive extraction. No Aegis application service, authority, credential, or runtime session is constructed.

A concrete integration defect was found after the first passing distribution tests: the real `hermes skills list` command created `skills/.hub/audit.log`, causing subsequent inventory to deny `undeclared_file`. The repair separates `.hub` into one fixed managed operational-metadata location outside immutable generation content, preserves it across update/rollback, and validates the exact link rather than reading metadata as evidence. Source/archive validation remains unchanged in its symlink prohibition. This was a second observed substantive repair/check turn, not a reset of unknown historical turn use.

Final focused verification used Go 1.26.6 with GOPROXY=off, GOTOOLCHAIN=local, GOMAXPROCS=2, GOFLAGS including -p=1, GOMEMLIMIT=2GiB, and repository-local TMPDIR:

- `go test ./internal/skillbundle/... -race -parallel=2 -count=1`: PASS, including real subprocess exit before pointer publication, process-lock release, retained old inventory, retry, update/rollback, metadata retention and local-edit preservation.
- `go vet ./internal/skillbundle/...`: PASS.
- Standalone tool cross-builds for darwin/arm64 and windows/amd64: PASS (compilation only; Windows installation deliberately unavailable; macOS durability not executed).
- Exact skill archive for unchanged skills at HEAD 7fe043cfce6c4afb178c35a9e857f12e8b64bbfe: sha256:982a93b3665031c1da356c6bc9f38af30ac20f9bf6d031c56a438f2baea83715. Bundle content digest: sha256:14363bad0d0124dc8e09bd7fc0d00a68ae308f30d593b9ea879abc0b04713b0f.
- Standalone binary install and inventory invoked from the proof directory without source arguments: all 15 slugs read back. Public `hermes skills list` discovered all 15 as local/enabled; subsequent exact inventory passed after the metadata repair. This was not a filesystem-isolated portability or agent-operation test.
- The discovered Hermes executable `/home/silas/.hermes/hermes-agent/venv/bin/hermes` reports v0.14.0, outside the supported >=0.18.0,<0.19.0 interval. Its discovery result is not supported-runtime qualification. No runtime update, provider/model call or normal-profile install was performed.
- `git diff --check`: PASS. `git diff --exit-code -- skills go.mod go.sum`: PASS. The installer source remains an uncommitted dirty candidate; the archive identifies unchanged skill bytes, not a clean release of the new installer.

F8 is partially resolved only: transactional mechanics and local distribution checks are now implemented. Supported-runtime integration, installed agent behavior, all-domain portability/capability qualification and release-shaped exact-candidate proof are still unresolved. The implementation is not issue-252 acceptance. No commit, push, PR, merge, release or flow advancement is claimed.

Launch impact: README, CONTRIBUTING, CHANGELOG, SECURITY, threat model, architecture and skill-distribution guide were updated for the new utility and residual risks. LICENSE, CODE_OF_CONDUCT, quickstart, no-key demo, recording instructions, contributor backlog and release workflow were inspected for impact and left unchanged; their workflows were not recertified. No release binaries/checksums or retained recording were fabricated or replaced. Private security/conduct contacts remain owner-controlled community-release questions, not a feature-PR veto. Full local/installed/recording/release verification remains with final_verification.

Judgments confirmed: filesystem packaging proof alone missed real Hermes metadata writes; a separate operational-metadata class avoids weakening immutable content checks. Uncertain: supported-runtime discovery/behavior and macOS durability. Missing evidence: complete all-domain contracts/operation matrix, exact #248/#245 adapter reconciliation, supported installed behavior/canaries, runtime binding/execution and full candidate proof. Pattern that worked: reproduce real discovery followed immediately by exact inventory; preserve the original failed proof home and use a fresh task-owned home for the corrected installed layout. Human review still required only for separately controlled release/publication decisions and any genuinely unavailable approved proof prerequisites.
