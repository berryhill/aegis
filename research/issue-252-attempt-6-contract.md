# Issue 252 attempt 6: portable suite contract

Domain obligation: advisory skill distribution must give an installed consumer exact public operation contracts without granting identity, authority, credentials, or execution. Product services remain the only authority for authentication, stanza selection, approval, mandates, runtime effects and evidence. Never union stanzas, invent transport, or treat an installed inventory as behavioral acceptance.

Candidate lineage: t_7a1e3ca/issue-252; baseline c923992451a82976716b9d1cce70fa0234ffe6f8, tree 99d8cce4b0a2dd2b4e21bda0652e2e0e7a655383. Authenticated TaskQueue-backed FlowStore readback matched ACTIVE session sess_3807637e42dc, cursor 4, loop attempt 6, packet tflp_29328504e0894ddd / f9b66139314fca6fe7fc587d6ed465e580abedeae1901d25b6ca18a9a608fca9. Earlier task prose mentioning 7fe043c is historical; the authoritative packet matches current source.

Beyond-target repair exception: F2/F3/F6 is reproducible in skills/aegis-skills.json: the operation table only contains aggregate domain routing entries, not the complete public command contracts. docs/HERMES_SKILLS.md explicitly reports incomplete portable builders and installed behavior. F5 requires exact delivered-interface reconciliation: origin/main contains #254 but the candidate predates it. F1/F4/F7/F8/F9 and FV-EXACT-CANDIDATE-PROOF remain unresolved at entry. No earlier evidence is waived. Cumulative prior turns remain unknown; two prior distribution repair turns are observed, not a total lineage count.

Acceptance boundary: full-suite source/transport/authority/test mapping and portable decoder-qualified contracts; representative installed successful and denied operations; authoritative runtime binding, expiry-backed reclaim and disposition; exact-candidate transaction/inventory evidence and supported-runtime qualification. Structural, request-shape, service/real-store, runtime-protocol, actual-agent, inventory and publication evidence remain separate. Changes invalidate corresponding source/packaging proof; final_verification retains complete-suite ownership.

Non-goals: new tasks, model/provider changes, shared-runtime changes, normal-profile installation, release publication, second state writers, generic credential-bearing clients, bypassing historical authenticated-home denial, resetting the branch or altering prior packets. Preserve =0.18.0, and research/issue-252-implementation-evidence.md byte-for-byte. Community release is separately owner-controlled and alone does not veto a feature PR.

Prerequisite observation: PATH exposes two Hermes names resolving to /home/silas/.hermes/hermes-agent/venv/bin/hermes, which reports v0.14.0, outside >=0.18.0,<0.19.0. This is not supported-runtime proof. Go is not on the initial PATH. Subsequent exact-path discovery resolved /home/silas/go/bin/go; its native compiler is 1.23.5 and the cached selected GOTOOLCHAIN=go1.26.6 reports go1.26.6. The initial multi-path version probe did not identify a path, so an early /usr/local/go assumption was rejected rather than used as evidence. No runtime or model change was made.
