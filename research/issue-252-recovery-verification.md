# Issue 252 recovered attempt-6 verification

Scope: implement_minimally, task t_7a1e3ca, request req_1fb29a1, attempt ta_t_7a1e3ca_req_1fb29a1_73, session sess_3807637e42dc. This is evidence, not a replacement plan or acceptance decision.

Custody: entry HEAD c923992451a82976716b9d1cce70fa0234ffe6f8 and MERGE_HEAD c932a28e8ee88b943e73cc3e8b83e286f59c20f8 matched the supplied recovery context and prior-state.json prior_spawn_log_tail. Reviewed combined parent diffs for CHANGELOG.md, docs/THREAT_MODEL.md and skills/aegis-skills.json. Retained working resolutions preserve both parents plus recovered work. Manifest validator passes. Unmerged stages were removed by staging reviewed resolutions, not by discarding either parent. Historical packet tflp_29328504e0894ddd remains unchanged.

Concrete beyond-target defects: the exact command `go test -parallel=2 ./internal/api -run '^TestDQ' -count=1` reproduced `distributed_queue_execution_test.go:195: fleet immutable record conflict`. The claim guard recognized initial submission and retry but not the canonical awaiting_runtime-to-queued binding. After that repair, the test exposed its wrong HTTP expectation (actual retry route returns 200, not 201), then a second product conflict after two claims/two attempts. Completion compared runtime audit mandate to workspace submission mandate instead of the immutable runtime binding. Minimal fixes reload exact canonical bindings; no fresh-authority gate or evidence-success requirement is removed. The fixture expectation now matches the shipped HTTP handler.

Parent-verified results, Go 1.26.6 linux/amd64, GOMAXPROCS=2, GOFLAGS includes -p=1, GOMEMLIMIT=2GiB:

- `go test -race -parallel=2 ./internal/persistence/fleet/badger -run 'Test.*(Claim|Completion|AcceptedSubmission)' -count=1`: PASS. Includes missing-binding projection denial and existing claim/completion regressions.
- `go test -race -parallel=2 ./internal/api -run '^(TestDQ|TestDistributedProtectedIntakeContracts)' -count=1`: PASS. Real-store peer-authenticated workspace submission, runtime binding via shared application service, live-lease denial, expiry-backed reclaim, second claim, authoritative failed disposition without output, and immutable replay. Synthetic session process and incompatible NoKeyAdapter are explicitly not supported-Hermes success proof. Installed-artifact protected metadata decoder checks are not actual-agent behavior.
- `go test -race -parallel=2 ./internal/command -run '^TestDistributedOperationMatrix$' -count=1`: PASS. Non-race verbose readback counted 15 owners, 97 public CLI nodes, 100 HTTP registrations, one hidden exclusion. Structural inventory only.
- `go test -parallel=2 ./internal/skillbundle/... -race`: PASS.
- `make -j1 skillbundle-verify`: PASS, 15 skills, 101 structurally valid cases; behavioral/service/runtime execution explicitly not_run in evaluator output.
- `git diff --check`, cached diff check and empty unmerged-index readback: PASS.

Independent read-only review: Hermes subprocess session 20260914_150151_3e0ce5 exited 0. It independently identified the missing binding eligibility branch, exact immutable/timing requirements and retained authority/dependency guards. It did not run tests or write files; parent verified the actual patch and regressions.

Runtime discovery supersession: historical PATH v0.14.0 observation is not current. Fresh `hermes --version` reports v0.21.3, still unsupported. The retained recording disclosed an existing proof executable at `/home/silas/.hermes/profiles/xander/workspace/codebases/aegis/.scratch/runtime-proof/hermes-agent/venv/bin/hermes`; exact execution of `--version` reports v0.18.2. This resolves only missing executable discovery, not capability, installed-agent or live-model qualification. No runtime installation/update, model change or normal-profile install occurred. An initial `hermes version` probe failed because the supported flag is `--version`; it is not runtime acceptance evidence.

Launch impact: changed CHANGELOG.md, docs/THREAT_MODEL.md and docs/HERMES_SKILLS.md for the newly verified consistency fixes and limited evidence class. Reviewed root README workspace boundary, SECURITY workspace/fleet consistency boundaries, CONTRIBUTING proof requirements, architecture binding-to-claim edge, quickstart, no-key demo, recording instructions/capture, Apache LICENSE, conduct policy, repository-local contributor backlog and release workflow. No CLI syntax, compatibility range, installation algorithm, architecture component or release workflow changed in this repair. Those retained assets need no additional repair-specific edits. Their merged incoming changes are preserved. Recording remains historical and explicitly excludes workspace Queue proof. Required full launch workflow replay, exact-candidate release binaries/checksums, installed/browser/runtime proof remain unverified and must not be inferred from these focused tests. Missing private security/conduct contacts and publication are community-release owner decisions, not newly waived gates.

Preserved artifacts: =0.18.0, SHA-256 e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855; research/issue-252-implementation-evidence.md SHA-256 3b71bf015b8f45099f134685b27420e421b6520d04d0c8a7b8fd96e0559c714d. Both match recovery manifest and remain outside the commit.

Candidate-turn ledger: lineage t_7a1e3ca/issue-252; cumulative turns unknown, never zero; two historical turns observed in packet. This recovery adds one concrete repair work cycle covering the reproduced linked claim/completion defects and focused checks, with the HTTP fixture correction part of that cycle. No claim of a reset budget.

Remaining ledger: F1 actual installed-agent behavior/ambiguity/renewal/canaries unresolved; F2/F3/F6 all-15 structural ownership coverage verified but complete portable decoder-qualified domain builders unresolved; F4 binding and positive expiry-backed service reclaim now verified (not public binding transport); F5 protected-intake delivered contract reconciled, broader delivered-interface reconciliation unresolved; F7 service handoff through authoritative runtime failure now verified, successful installed runtime execution unresolved; F8 transaction tests pass, exact-current-commit archive/inventory verification pending; F9 supported executable found, capability/installed-runtime qualification pending; FV-EXACT-CANDIDATE-PROOF complete suite/browser/installed proof pending; COMMUNITY-RELEASE-OWNER remains separate. No publication/merge eligibility or complete issue acceptance is claimed.

Judgment: the prior description of a fixture-only conflict was disproved by tracing two product persistence checks. Narrow positive-path integration exposed defects that strict decoder and historical readback tests did not. The effective remediation retained immutable provenance and used runtime binding rather than weakening authority checks. Missing evidence remains explicit.
