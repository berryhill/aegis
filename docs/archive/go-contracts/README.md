# Archived Go Contracts

These `.go.txt` files preserve the pre-implementation `specs` Go package for historical provenance. They are not compiled, imported, or normative.

The package duplicated domain models and transport-neutral interfaces before the production implementation existed. Keeping it active would create two competing model definitions. Current normative contracts are Markdown under `specs/`; current executable domain types and services live under `internal/`.
