# Terminal Recording

The recording source is `scripts/demo-no-key.sh`. It uses ignored disposable paths inside the checkout, initializes an empty generation-managed Badger authority only in its disposable workspace, derives only the local UID/username, sets the copied configuration and charter to mode `0600`, prints no credential values, and removes its workspace and demonstration executable.

The retained recording is:

- `docs/assets/aegis-no-key.typescript`
- `docs/assets/aegis-no-key.timing`

Replay it with:

```sh
scriptreplay --timing docs/assets/aegis-no-key.timing docs/assets/aegis-no-key.typescript
```

It was captured from an older revision of the real script with Hermes Agent 0.18.2, replayed locally, and reviewed for usernames, home paths, temporary paths, bearer tokens, and key-shaped values. Local identity and home paths are placeholders, configuration displays `[REDACTED]`, and the expected no-provider result is not presented as model success. The current script now additionally reports `aegis version dev` and uses the enforced checkout profile path. Current interactive onboarding also precedes this historical capture: it identifies the concise `basic` presentation by default, offers `details`/`advanced` before presentation-owned approvals on the same deterministic trust path, and uses textual terminal roles (`STATE`, `ACTIVE`, `QUEUED`, `ERROR`, `ACTION`) that remain meaningful without color. The current launch configuration additionally uses generated token-file custody, daemon singleton ownership, the non-launching `aegis console` client, optional explicitly approved `aegis gateway` lifecycle commands (with `aegis service` as a compatibility alias), and bare development/production paths that ensure authenticated gateway readiness and enter the authenticated Aegis agent terminal directly after fresh activation. The recording contains none of those newer bootstrap, gateway, or browser interactions. Their bounded built-binary PTY proof is `go test ./cmd/aegis -run TestBasicAndAdvancedBootstrapRoutesReachSameArtifactDerivedStateAndResume -count=1`; width/color hierarchy remains covered by focused `internal/tui` tests. These retained files are historical and must be regenerated with supported Hermes before publication. They are not current launch evidence and this documentation update does not make them current.

To record with asciinema when available:

```sh
asciinema rec --idle-time-limit 2 --command './scripts/demo-no-key.sh' aegis-no-key.cast
```

Before publishing any recording, replay it, inspect it for usernames, hostnames, paths, tokens, API keys, and provider output, and regenerate it whenever CLI output or commands change. Do not edit a recording to imply a successful provider-backed turn.

The retained no-key recording does not enter the interactive manager, open a credential authority, or invoke pinentry. It therefore does not demonstrate the Core 15 slash surface, the `/agents` Agent Registry extension, fresh activation's direct agent-terminal handoff, or protected authority prompt. It also predates the separately approved canonical built-in `aegis` Agent registration, expertise projection v2, discoverable `/agents` header, authoritative natural-intent guidance, and typed manager turn-failure categories. Do not use or edit the historical capture to imply those paths ran; a future regenerated recording must show the approval/readback without exposing principal identifiers or digests and must state that registration grants no credential/model authority. It is currently stale because the recording source and no-key CLI output changed. A future authority recording must use a fake pinentry and generated canary, never a real passphrase or operator helper. Any future manager recording must capture both rich and `AEGIS_ACCESSIBLE=1` paths, show the permanent untrusted-model origin label and trust surface, and use only fake local runtime fixtures unless an already-installed live artifact is separately authorized.

The opt-in [operator acceptance POC](OPERATOR_ACCEPTANCE_POC.md) is separate from this publishable terminal recording. It retains bounded sanitized JSONL evidence from the real accessible/plain manager PTY, never a protected value, and fails if its generated canary reaches capture or metadata. Its evidence can contain local model prose and credential metadata and must not be committed or published without manual review. A hermetic fake-PTY run proves the recorder but is not a live transcript.

The installed-MVI verifier used by CI and the tag-triggered release workflow is non-interactive and intentionally not represented by this no-key Hermes recording. Its canonical local evidence is the exit-checked output of `./scripts/verify-installed-mvi.sh`: one exact clean tracked source revision; four release-shaped archives each constrained to exactly one root `aegis` regular file with mode `0755`; verified VCS metadata, checksums, embedded version, and embedded source revision; fail-closed first run; an extracted-binary Registry/Loop/Graph/Queue/Hermes/evidence/disposition proof with a registered fake gateway, gateway-invocation evidence, disposable-home cleanup, durable rejection, and historical readback; and an extracted-binary console proof covering generated protected token-file transport, second-daemon singleton denial, `aegis console` direct password-gate discovery, browser-authenticated Agent registration, and a deterministic complete Agent/Loop/Graph/Queue plus artifact/receipt/disposition fixture. Real headless Chrome authenticates with a generated canary principal password; follows rendered exact-related-record links across the immutable chain; verifies canonical direct load, reload, Back/Forward, server-side Loop search/lifecycle filters, and filter-preserving pagination; closes the exact inspector; rotates the principal password and verifies old-session invalidation; signs out; and rejects CSP violations, JavaScript errors, request failures, and unexpected HTTP 500 responses. It also verifies that the console document loads no executable client JavaScript while the retained self-hosted Datastar asset remains directly servable and absent from unauthenticated and authenticated documents. It uses and removes isolated repository-local proof state, starts no real user service, and makes no systemd/linger evidence claim even when an explicit empty archive-output directory is retained. This does not authenticate the source publisher, make the historical recording current, or prove release publication.

Principal-password enrollment, login, throttling, authenticated rotation, and old-session invalidation are also not demonstrated by the retained no-key recording. Current proof must come from focused Go tests, `python3 scripts/console_security_test.py`, and the installed browser acceptance path using generated canary passwords; recordings must never capture password values, verifier digests, cookies, CSRF values, session identifiers, or process arguments containing secrets.

The pre-publication release-candidate verifier is also non-interactive and does not make the historical recording current. Its retained evidence manifest, exact digests, and workspace-only rollback/withdrawal rehearsal are the relevant surfaces; do not edit or regenerate the no-key recording to imply candidate approval or publication.
Execution Queue lifecycle acceptance is not inferred from the historical no-key recording. It requires deterministic installed-binary and browser-console proof for retry/reclaim causality, lifecycle filters, timeline, eligibility/denial states, keyboard focus, and responsive rendering. Any new visual recording or screenshot packet must be captured from the exact committed candidate; no such artifact is fabricated by documentation updates.
