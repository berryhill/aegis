# Issue 234 — launch-asset review

## Attempt 9 / execution 101 — source-review follow-up

A bounded independent same-provider/model read-only reviewer found no production
CSS/reset regression, but correctly identified that harness cleanup could unlink
an unreserved shared socket path. The harness now allocates an exclusive 0700
short child directory, keeps socket/lock cleanup inside it, and rejects excessive
path length before allocation. Two regression guards cover namespace allocation
and pre-allocation length denial. CONTRIBUTING explicitly distinguishes the Go
initializers' inherited build/cache environment from the isolated candidate
server environment. No system or runtime configuration was changed.

The reviewer also identified missing independent version-revocation readback.
This remains an evidence limitation. A proposed active-inventory exclusion check
failed because the HTTP operation revokes an exact version, not the record; it
was removed rather than changing production semantics or claiming record revoke.
The retained harness proves the reviewed revoke response, not complete immutable
version-revocation readback. This is separate from responsive navigation proof.

Candidate `01f367631cf2904dca44ea67cd7b5baca4eaff96` was built from a clean
isolated proof clone and extracted from a validated native archive. Its populated
principal-authenticated Credentials journey passed at 1440/900/390, including
visible sign-out, no horizontal overflow, navigation, denial and native fallback.
This is historical to the following harness-only safety correction; successor
proof must be rebound. The initial archive was correctly rejected for file mode;
normalizing the staged executable to the required 0755 produced a valid archive.
No release guard was weakened. Independent visual and runtime acceptance remain
unresolved; no publication is authorized.

## Attempt 9 / execution 101 — successor local candidate preparation

The canonical session readback confirms the original task, active cursor 4,
attempt 9 and packet `tflp_3901042140074c95` with the injected hash. Canonical
`may_commit_code` and `may_mutate_code` are true. Effective Git identity is now
Xander / xander@agents.tarxan.io; execution-99 contamination is historical,
not a remaining local commit blocker. Publication denial remains unchanged.

The existing reset repair, generated-material harness and Credentials-only
390px header spacing repair are preserved. The installed harness additionally
requires the authenticated sign-out control to be visible and wholly inside
the viewport in collection mode, rather than accepting clipping as a fix.
Fresh source Chrome coverage passed at 1440/900/390, including column thresholds,
restoration and native fallback. Focused API/console/reset tests and three Python
guard tests passed. These results are not extracted-candidate acceptance.

Two preliminary harness invocations did not reach the browser: the first lacked
Go on PATH, and the second used the historical execution-100 development binary,
which correctly denied its execution-profile home/repository mismatch. Neither
failure is runtime qualification or a new Credentials UI regression. No guard
was weakened. Exact successor archive proof must use a clean source and a
version-bound extracted binary, not that development executable.

No supported-Hermes demonstration was retried and no runtime/provider/model,
operator state, Git identity configuration, external PR or publication was
changed. Independent visual acceptance, final supported-runtime qualification,
owner reporting decisions and historical uncertainty remain outstanding.


## Attempt 9 / execution 99 — implementation progress, acceptance denied

Domain/control contract: replace synthetic-only evidence with a generated-material,
principal-password-authenticated Credentials journey through the extracted binary;
keep server admission authoritative and preserve the existing reset effect-scope
repair. No operator runtime, provider/model configuration, normal credential state,
shared Git identity, publication gate, or task topology may change.

Input packet remains `tflp_3901042140074c95`, hash
`35dd48f69432ce73a1f493f33128cf06cf53d2bd9b5f556249a3ccdd9020928b`.
HEAD/branch still match `5d639c07aeb0834c6ed708bf11940e58cecef67c` /
`task-t_0ab23a2-234`. The exposed tools do not include canonical flow/memory or
subagent calls; no new canonical store readback or independent-agent review is claimed.

Added `scripts/verify-installed-credentials.py`, a fresh-directory custody helper,
and three harness guard tests. The harness does not invoke or fabricate Hermes.
It starts only the supplied candidate in isolated proof state, authenticates a
generated principal password, creates thirty records through review/execute,
rotates one selected record twenty-five times, and revokes a separate record.
Review non-mutation, generated-value non-disclosure in operation responses,
unauthenticated/missing-CSRF/cross-origin/replayed-receipt denials, semantic visible
columns/cells, desktop pane proportions and independent scrolling, history,
filter/scroll/focus restoration, narrow single-pane behavior, unknown-record
denial, and no-script/denied-storage native links are exercised. This is not yet
exhaustive binding/backup/cross-session/expiry/adversarial installed acceptance.

Direct checksummed archive extraction and `go version -m` bind the exercised
binary to clean attempt-8 HEAD, not the new dirty source. Binary SHA-256:
`73a68dd057ebc66e8b6587161d35b9dec17afba2f0842700e05f4398375d73e3`.
All five entries in the retained `dist-corrected/SHA256SUMS` verified. The accepted
artifact independently matches 755716 bytes and the supplied SHA-256; offline
Chrome loaded `#/credentials` with network blocked at 1440/900/390. Paired
screenshots are captured, not independently image-reviewed visual approval.
Evidence: `.scratch/credentials-ex99-q/result.json`, `geometry-*.json`,
`credentials-*.png`, `accepted-*.png`.

The new installed proof correctly exits 1: at 390px the authenticated header's
sign-out form extends the document to 428px. This was absent from the prior
synthetic acceptance assertions. A Credentials-only <=420px spacing correction
preserves status, rotation and sign-out controls. The strengthened current-source
Chrome regression reproduced overflow (including an insufficient first spacing
repair at 396px) and now passes at 1440/900/390 with all existing routing, scrolling,
column-threshold, resize and fallback checks. No CSS overlay or altered archive
is presented as installed proof. Successor clean-candidate archive verification
and authenticated comparison must rerun after the source can be committed.

Other current checks: three Python harness guard tests passed; uncached console,
API and credential package suites passed; focused reset tests passed with the
pre-existing uncommitted decline/EOF/cancellation repair preserved. No broad
release fixture suite was run under the contaminated shared Git identity.

Incremental launch impact review: CHANGELOG and CONTRIBUTING now describe the
harness and qualification limits. README, LICENSE, SECURITY, CODE_OF_CONDUCT,
threat-model and architecture boundaries/diagram, QUICKSTART, no-key docs,
recording docs and retained capture/timing, release workflow/checksums, and local
contributor backlog were inspected for this change and require no further edits.
The existing reset/quickstart modifications remain intact. This is impact review,
not exhaustive launch-command execution or confirmation of every historical claim.
No-key demonstration was not retried, no runtime was installed or replaced, and
no external issue/release/PR action occurred.

Remaining gates are explicit: shared Git identity still reads `Release Test` /
`release-test@example.invalid`; controller restoration is required before commit.
New-source exact-candidate installed acceptance and independent design/security
review remain outstanding. Supported-Hermes no-key qualification is still an
unresolved final gate, not a reason to suppress independent local engineering.
Owner reporting decisions and historical diagnostic uncertainty remain unchanged.
Publication remains denied. Confirmed judgment: a populated authenticated harness
finds a real shell overflow that fixture-only checks missed. Generated plaintext
fixture input, transport tokens and KEKs from this execution were removed after
process shutdown; encrypted fixture state is retained only as private task evidence.

## Attempt 8 implementation impact (not launch acceptance)

The current implementation step consumes packet `tflp_a767f8d240c84c5b` / `0ae38d2d97740310d8900b74b4b879bb959ba400426d0ff3b331b143bdef84ff`. Canonical readback matched active attempt 8 and the original task/session/branch. Historical review sections below remain attributed to their original candidates.

The accepted artifact independently matched its supplied 755716-byte identity and SHA-256. Offline Chrome with HTTP/HTTPS blocked confirmed accessible Reference/Status columns at 390px and four columns at 900/1440px. CSSOM inspection established two inventory-container thresholds (Version <=540px, Kind <=420px) plus the <=760px viewport Version rule; the <=900px single-pane breakpoint was already correct. The repair implements those container/viewport rules only inside Credentials, without changing authority or routing. The strengthened browser regression reproduced the real four-versus-two-column defect before repair and passed afterward. Earlier test-development runs had textContent/innerText expectation mistakes; those logs are retained and are not product-defect proof.

An independent read-only Hermes review confirmed the revoked KEK copy and quickstart reset decline defects. Parent source readback verified `internal/command/reset.go` preparation at lines 71–87/121–128 precedes destructive confirmation at 189–196; purge, permission repair and deletion are later. Revoked copy now describes only rendered record metadata/version history. Quickstart explicitly preserves an already approved preparatory stop when destructive deletion is declined. The companion credential setup guide also had stale vault/KEK inspector wording and now describes the actual metadata and column disclosure.

Incremental asset inspection: README, LICENSE, SECURITY, CONTRIBUTING, CODE_OF_CONDUCT, threat-model boundary/diagram, architecture diagram, no-key script/docs, recording/docs, release workflow, and contributor backlog have no additional presentation-repair edits. CHANGELOG, QUICKSTART and CREDENTIAL_AUTHORITY_SETUP are updated. No CLI syntax, release packaging, runtime identity, authority path, diagram edge or recording behavior changed. Retained recording remains historical; owner private security/conduct reporting decisions remain unresolved. New exact-candidate archives/checksums, fresh supported-runtime no-key execution, populated principal-authenticated extracted-binary Credentials comparison at 1440/900/390, broad verification and final review are NOT established by this implementation-step inspection. No release or remote contributor issue was created.

Focused proof: pinned templ generation succeeded; uncached Chrome-enabled console/API suites passed; existing focused reset decline/preparation tests passed; static and adversarial console-security checks passed; diff check passed. Browser coverage now asserts accessible headers AND visible row cells in browse/selected modes, preserves version-history columns, exercises viewport 760/761 and container 420/421/540/541 boundaries, and restores columns when resized wider, in addition to the existing navigation/scroll/focus/fallback/adversarial checks. These are synthetic rendered fixtures, not installed authority evidence. Evidence: `.scratch/attempt-8-browser-red-actual.log`, `attempt-8-browser-green.log`, `attempt-8-focused.log`, `attempt-8-reset.log`, `attempt-8-independent-review.log`.

Finding reconciliation: narrow column source defect, revoked inspector wording and quickstart decline wording are repaired with bounded source/fixture evidence. Populated authenticated installed proof and current no-key reproducibility remain required verification, not waived or reclassified as owner gates. Owner reporting decisions remain separate; historical subprocess/PTY uncertainty remains unexplained. Publication denial remains in force until the remaining substantive checks and applicable gates pass. Judgment confirmed: accessible column presence differs from DOM presence, and container breakpoints matter even on desktop. No production activation or authority expansion occurred.

## Attempt 5 launch review

Review activity completed; launch approval is not granted. Packet input: `tflp_5a02ad8b75fc45e7` / `0f4002439e7991fd642e295146267d858797998b8853aeaf10813f4cf8a37366`. Branch/base and dirty source remain preserved. An attempted read through the shared FlowStore helper was blocked by fleet authority; that path was stopped without retry or raw-database fallback. Therefore this review does not claim fresh canonical flow readback.

Required assets inspected: README, LICENSE, SECURITY, CONTRIBUTING, CODE_OF_CONDUCT, CHANGELOG, threat model, architecture diagram, quickstart, no-key script/documentation, recording/documentation, release workflow/verifier, and contributor backlog. LICENSE remains unaffected. Owner reporting policies remain unchanged. Existing implementation updates accurately distinguish bounded Credentials presentation, unconditional stanza ambiguity denial, verified source-build stamps, and dirty-source versus release evidence. The diagram retains the corrected credential-authority-to-service metadata edge. Contributor proposals remain local, not published issues. No exact-candidate release binaries/checksums or installed-browser acceptance exist from this review.

An independent read-only reviewer identified two documentation defects, both directly checked and corrected here: quickstart cleanup omitted daemon shutdown and incorrectly said all repository reset targets are denied; architecture understated the separate development gateway-stop approval. Quickstart now limits manual cleanup to disposable state after verified shutdown and distinguishes exact development-profile reset from custom example files. Architecture now preserves the preparatory stop as a separate approved effect even if deletion is declined. Parent review additionally qualified the architecture's passphrase wrapping paragraph as the explicitly selected passphrase-file path, not default host-file custody. These are documentation corrections only; no production code or runtime lifecycle was changed. Source evidence: `internal/command/reset.go:65-87,121-147`, `internal/reset/reset.go:1085-1119`, and `internal/command/bootstrap_presentation.go:126-127`.

Fresh checks passed: module download; verified source build; nested-worktree provenance regression; pinned vendor verification; static and adversarial console security; templ generation with unchanged generated-file digest; broad Go tests and race tests (cache eligible); vet; formatting; installed-MVI and candidate denial tests; 33 archive/fleet/recorder/browser-harness Python tests; uncached Chrome-enabled console/API tests; uncached reset tests; skill-bundle validation/evaluation. Govulncheck passed its called-symbol scan using automatically selected Go 1.26.8, while reporting two imported-package and four required-module vulnerabilities not called by this code. The development binary independently reports the intended base revision and truthful `vcs.modified=true`.

The real no-key script passed with supported Hermes 0.18.2 and a cleared provider environment, stopping at the authentic missing-provider boundary. Recording replay passed; retained bytes are still qualified attempt-3 development evidence, not a recording of the new builder or exact candidate. Accepted artifact byte/hash readback matches 755716 bytes and the supplied SHA-256; no new offline visual or installed comparison is claimed in this step.

`verify-installed-mvi.sh` returned expected exit 2 for dirty tracked source. `git diff --exit-code -- web/console go.mod go.sum` returned expected exit 1 for the implementation diff; unchanged generation digest is the relevant no-drift evidence. Neither result proves installed acceptance. Exact-head release-readiness and candidate archives/browser comparison require the later clean committed candidate. Interactive onboarding, live certification, destructive cleanup/reset, service installation/activation, update, release, and external issue commands were not applied to the operator environment. No owner decision record was fabricated. Complete executable launch acceptance is not claimed.

Evidence: `.scratch/attempt-5-launch/{checks.log,demo.log,replay.log,browser.log,vuln.log,reset.log,independent-review.log,generated-before.txt,generated-after.txt,skills-validate.log,skills-evaluate.log}`. The reviewer used a bounded separate Hermes session and made no writes. Owner final launch gates remain Matt's private vulnerability-reporting confirmation and private conduct-reporting destination. These are not source-repair prerequisites. Historical subprocess/PTY failures remain unexplained; current passing checks do not erase them.

Judgment: focused source tests can pass while destructive-cleanup documentation is still unsafe. Correct the prose against exact lifecycle controls, preserve prior approval effects, and never exercise retained-state deletion merely to validate documentation. Final acceptance still needs a clean candidate and exact authenticated installed responsive/design comparison; this review cannot supply those by relabelling fixtures.

## Attempt 5 implementation readback (current incremental evidence)

This records the current implementation step, not completion of later test/review/publication gates. Packet `tflp_5a02ad8b75fc45e7` / `0f4002439e7991fd642e295146267d858797998b8853aeaf10813f4cf8a37366` matched active FlowStore attempt 5. The original task branch, worktree, dirty Credentials implementation, and base HEAD are preserved.

- Reproduced the selector defect with the strengthened regression: requested `principal` was allowed despite two authorized matches. The selector now rejects ambiguity before request filtering. Focused core/app tests and uncached app race tests pass, including zero matches, unknown and known-but-unauthorized requests, all requested variants of ambiguity, and full selected-stanza equality (no grant/scope union).
- Re-read and hashed the accepted HTML: exact supplied digest and 755716 bytes. Offline Chrome opened `#/credentials` at 1440 and 390 pixels with HTTP/HTTPS blocked, all four headers present in the DOM, and no page errors. This is input inspection, not candidate visual acceptance.
- Direct Go source inspection confirms this toolchain's VCS root detector recognizes `.git` directories, not linked-worktree `.git` files. The historical binary still embeds the enclosing Hermes revision. `scripts/build-source.sh` now verifies its own root, clears inherited foreign Git bindings, pins Git subprocesses to the actual worktree, builds with VCS stamping enabled, and checks Go's embedded revision/time/dirty status before publishing the binary. The actual task build now embeds `d8dd759ab86c52ee59f568b08017c456e4876dff` with truthful `vcs.modified=true`. No toolchain modification or forged stamp is used.
- The real-Go regression creates distinct parent/source repositories and a nested registered worktree. Parent/root invocations with inherited foreign Git bindings yield the source revision and clean flag; dirty source yields the same revision and dirty flag. An isolated adversarial no-stamp compiler shim is rejected without replacing the last verified output. `Makefile` and release CI run this regression; demo and installed archive building share the helper. This checks metadata binding, not immutable filesystem snapshots, reproducible builds, arbitrary build-flag safety, or publisher authentication.
- A short-lived independent read-only reviewer found no substantive defect in the selector or provenance helper. Its known-but-unauthorized request suggestion was implemented and tested; parent readback also strengthened full-stanza equality. The separate first provenance investigator reached its bounded iteration limit and supplied an advisory summary, not a successful verification run.
- Static security, adversarial security tests, pinned vendor check, uncached Chrome-enabled console/API suites, broad Go tests, broad race tests, vet, eleven demo/archive/fleet/recorder Python tests, installed-verifier denial tests, and diff-check passed. Broad runs are cache-eligible. The changed app tests were repeated uncached after the final assertion changes. No recurrence of the historical bus error or PTY interruption was observed; neither root cause is newly established.
- The no-key script ran with the supplied supported Hermes 0.18.2 executable and an empty provider environment, reaching the real `No inference provider configured` boundary. The retained sanitized attempt-3 capture replayed; its demonstrated CLI syntax is unchanged, and recording documentation now explicitly distinguishes it from the new internal build check. It is not exact-candidate evidence. Running `aegis version` under the dispatch's synthetic HOME first denied because the checkout is outside that home; with the host operator-home anchor it reports `aegis version dev`. No runtime state or user service was activated.
- The installed verifier still exits 2 for dirty tracked source, before packaging. This is the expected gate, not accepted installed evidence. Untracked non-ignored task logs also make the Go dirty flag true: later publication must explicitly stage intended source/tests/docs and keep logs/scratch out of the clean candidate rather than weaken the guard.

Incremental asset review: updated README/quickstart/contributor/operator-acceptance build commands, AGENTS guidance, Makefile, release test workflow, source build/demo/installed scripts, SECURITY technical prose, threat model, architecture selector description, changelog, no-key/recording qualifications, and task contract/evidence. The corrected credential-authority diagram edge remains intact. LICENSE and CODE_OF_CONDUCT are unchanged; owner reporting policy was not modified. Contributor issue proposals remain repository-local and unaffected by this repair. The recording bytes remain the qualified attempt-3 capture. No new release binaries/checksums, remote issue, PR, release, publication, or merge acceptance is claimed; candidate archives/checksums and exact installed authenticated responsive comparison remain later gates.

Evidence: `.scratch/attempt-5/selection-red.log`, `authority-final.log`, `authority-race-final.log`, `provenance-test.log`, `aegis`, `browser-api.log`, `go.log`, `race.log`, `vet.log`, `demo.log`, `replay.log`, `installed-guard.log`, `implementation-review.log`. Scratch evidence must not be staged wholesale.

Judgments confirmed: requested-stanza narrowing violated the unconditional ambiguity rule; `.git` file detection explains the wrong embedded revision. Effective remediation: ambiguity-before-narrowing plus real nested-worktree regression and checked Git binding. Uncertain: historical subprocess bus error and interrupted PTY run causes. Missing evidence: clean committed candidate, exact installed-browser comparison, CI and merge. Human review: Matt still must confirm the private security route and designate a private conduct route before launch; these are not source-repair prerequisites.

## Attempt 4 implementation readback (historical)

The carried diagram defect is repaired: encrypted bbolt authority supplies credential-record/version metadata to shared application services, and a principal-authenticated metadata projection reaches Console. Configured environment bindings still feed only the selected-provider adapter. Direct source readback of `internal/app/credentials.go:110-149` confirms the principal checks and Query/Metadata calls. No production code, CSP, fragment adapter, pinentry assertions, recording, or owner policy was changed in this step.

- Canonical full review outputs and attempt-4 previous-error identity matched `tflp_6853ea8140a645aa` / `d4a1fdb83a0a1d19bac90c62e3076fa7bfae81e36f7c47db84fe4680bc5c0532`. The original dirty task worktree and base HEAD remain intact.
- Accepted HTML bytes/digest matched the supplied artifact. Offline Chrome opened `#/credentials` at 1440 and 390 pixels, all four headers were present, and no page errors occurred.
- Static security, both adversarial source tests, pinned vendor verification, uncached real-Chrome-enabled console/API suites, broad Go tests, broad race tests, vet, build, and diff-check passed. Broad tests were cache-eligible; the entire broad race run used cached test results. Logs are under `.scratch/attempt-4/`.
- The exact historical lifecycle test passed five uncached repetitions without changing its nested build command or disabling VCS stamping. The prior bus error remains preserved in `.task-234-final-go.log`; root cause remains unknown and no recurrence was observed.
- Additional build provenance finding: `go version -m` on both new development binaries reports `vcs.revision=609cdf1877fbf6877626ae48da21e8878f432855`, not task HEAD `d8dd759ab86c52ee59f568b08017c456e4876dff`. `go build -x` shows VCS commands run in `/home/silas/.hermes`, whereas explicit Git readback at repository root and `cmd/aegis` returns the task HEAD. Relevant Git/Go override variables were unset. This is observed enclosing-repository provenance selection, not evidence of an exact candidate build and not an established cause of the historical bus error. Retain `.scratch/attempt-4/build-trace.log`; exact-candidate verification must resolve this environment qualification without disabling or forging provenance. No installed-candidate acceptance is claimed.

Incremental launch impact: only the architecture diagram and these task-owned evidence/contract records changed. Prior full launch-review results remain attributed to that review. README, SECURITY, CONTRIBUTING, CHANGELOG and demonstration instructions retain their existing behavior; LICENSE, CODE_OF_CONDUCT, release workflow, no-key script and installed verifier remain unchanged from base. A complete refreshed launch-asset review and executable workflows remain the dedicated later step, not a claim of this narrow repair.

Remaining gates: independent reviews; fresh final verification; exact committed-candidate archives/checksums and authenticated installed-browser acceptance, including the observed VCS provenance mismatch; exact-head CI; and Matt's private vulnerability-reporting confirmation and private conduct-reporting destination. Owner decisions remain final launch gates, not source-repair prerequisites. No commit, push, launch, or merge acceptance occurred.

Judgment confirmed: the diagram was wrong without requiring an authority implementation change. Uncertain: historical bus-error cause. Effective remediation: correct the source-to-service edge and independently read back the actual authenticated methods. Missing evidence: exact candidate provenance and installed acceptance, not ordinary compilation success.

## Attempt 3 implementation readback (historical)

This is source-repair evidence for `implement_minimally`, not completion of the later launch-review, publication, or merge gates. The attempt-1 review below is retained verbatim as historical failure evidence.

- Canonical FlowStore readback matched loop attempt 3 and previous packet `tflp_4505f79d62014093` / `f4db5ede32d86d7ef9fbf51e5797199e7b084ee5b63995dd7af2b54cefc3ea29`. The task branch, original base HEAD, dirty Credentials implementation, and flow identity were preserved.
- Accepted design was read and independently hashed (755716 bytes, supplied SHA-256). Offline Chrome opened `#/credentials` at 1440 and 390 pixels with no page errors and the four inventory headers present. No network design retrieval occurred.
- Pinentry diagnosis: a metadata-only `strace -f -tt -e trace=wait4,kill,exit_group` run of the two historically failing race tests with `-count=10` reproduced three failures at 2.01/2.03/2.10 seconds, with process-group SIGKILL in the retained trace. The same command passed after increasing only the fake test-binary budget from two to ten seconds. This demonstrates deadline interference under instrumentation; the exact split between startup, scheduling, and race-detector exit overhead was not measured. Production `pinentry.go`, its two-minute timeout, and fail-closed result handling are byte-identical to the base.
- An independent read-only Hermes reviewer accepted the bounded test-only correction and identified weak generic-error assertions. Those assertions now require the exact cancellation/protocol kind, post-interaction marker, and no returned protected bytes. Explicit 100-millisecond timeout coverage is retained. The reviewer also noted inherited limitations: the short deadline need not reach GETPIN before expiry, non-TTY fixtures alone do not prove an available fallback was not attempted, and post-data timeout/cancellation remains classified as a protocol error. No production classification change is claimed.
- Current launch prose now matches expertise v3, password-authenticated browser sessions, and default host-file custody in README, SECURITY, architecture, threat model, quickstart, demo, and contributor backlog. Owner reporting policy was not changed.
- The real no-key script ran with supported Hermes 0.18.2 in a provider-credential-free environment, passed discovery/version/charter/config, and reached the authentic missing-provider boundary. The regenerated `docs/assets/aegis-no-key.typescript` and `.timing` were replayed and reviewed, including a bounded token-shaped/home-path scan. Transcript SHA-256: `90e34999cdc7e8bb35f6d3532e961bd5f0e9409a29cdbd86ffa1d20aa8d1544c`. This is development-source evidence, not model success or installed-candidate proof.
- Asset impact: README, SECURITY technical prose, CHANGELOG, architecture, threat model, quickstart, no-key/recording documentation, recording bytes, and repository-local issue proposals were reconciled. CONTRIBUTING's fixture-versus-installed instructions remain applicable. LICENSE, CODE_OF_CONDUCT, release workflow, no-key script, and installed verifier were inspected and remain unchanged from the task base. No release binaries/checksums or external contributor issues were created; clean-candidate packaging remains pending.
- Focused verification passed: static security harness; two adversarial source tests; real-Chrome-enabled console and API suites; focused pinentry race suite; unchanged generated renderer; pinned vendor digest; and eleven demo/archive/fleet/recorder tests. The first Python demo test invocation lacked Go on PATH; rerunning with the discovered Go path exported passed all eleven, without source changes for that environment mistake.

- Broad verification after the final test-assertion edit passed `go test ./...`, `go test -race ./...`, `go vet ./...`, `go build -o .scratch/attempt-3-recording/aegis ./cmd/aegis`, and `git diff --check`. The changed command package reran uncached (41.692 seconds normally, 65.090 seconds with race instrumentation); other packages were cache-eligible. This clears the carried broad race failure for this source candidate without erasing its historical failures.

Retained evidence: `.task-234-pinentry-process.trace`, `.task-234-pinentry-process-after.trace`, `.task-234-attempt-3-independent-review.log`, `.task-234-attempt-3-demo.log`, and the attempt-3 Go/race/vet logs. Task logs and `.scratch` artifacts must not be staged indiscriminately.

Remaining gates: later independent security/launch/final reviews, exact committed-candidate archives/checksums and authenticated installed-browser acceptance, exact-head CI, and Matt's confirmation of private vulnerability reporting plus designation of a private conduct-reporting destination. Owner decisions are final launch gates, not source-repair prerequisites. No publication, activation, or merge acceptance is claimed.

## Historical attempt 1 review

Task: `t_0ab23a2`; flow: `sess_aegis_7ae48c1c5c7ffeba`; step: `review_launch_assets`; implementation-loop attempt 1.

Review activity completed. Candidate outcome: FAILED_RETRYABLE. Publication is not allowed. This is not task completion, installed-candidate acceptance, or merge evidence.

## Contract and accepted design

Credential selection and presentation restoration must not grant authority. The console remains metadata-only; authenticated server reads and separate reviewed-operation admission remain authoritative.

The supplied local artifact `/home/silas/.hermes/.scratch/xander-accepted-design-recovery/index.html` was independently read: 755716 bytes, SHA-256 `634e23fafafb5cd032a86ac80dbc3809d8e5bd9939cda80c16584beb4d4d2a9b`. Offline Chrome opened `#/credentials` at 1440px and 390px with no page errors. The document contains the Reference, Kind, Status, Version inventory headers. No remote design retrieval or artifact mutation occurred.

## Asset impact matrix

| Asset | Review and disposition |
| --- | --- |
| README.md | Updated fragment navigation claims: reconciliation performs a same-origin server-backed navigation, not zero network activity; native query-bearing links are the no-script fallback. Pre-existing passwordless bootstrap claims still contradict the current password-only contract and require correction. |
| LICENSE | Reviewed Apache 2.0 text; unaffected by presentation-only changes. |
| SECURITY.md | Added untrusted fragment/history and metadata-only prepare-link boundary. Existing private-reporting route remains an unresolved owner verification/decision, not an invented address. |
| CONTRIBUTING.md | Added Credentials regression coverage, explicit opt-in real-browser command, and fixture-versus-installed-proof distinction. |
| CODE_OF_CONDUCT.md | Reviewed; no task-specific change. Explicit missing owner-designated private conduct-reporting route remains a launch blocker. |
| CHANGELOG.md | Added current Credentials layout, concise detail, responsive and route/restoration behavior under Unreleased/Changed. |
| docs/THREAT_MODEL.md | Added non-authoritative fragment/history boundary. Existing trust-boundary diagram topology is unaffected by this presentation change. |
| docs/ARCHITECTURE.md | Added pane and server-navigation composition; existing console-to-service diagram edge remains unchanged. The diagram still names expertise v2 while current README/CHANGELOG name v3; this pre-existing launch drift remains unresolved. |
| docs/QUICKSTART.md | Added the actual Credentials workflow, 900px breakpoint, independent 42/58 selected desktop panes, native fallback and separate prepare workflow. Existing installed-proof paragraph still mentions obsolete bootstrap exchange. |
| docs/DEMO_NO_KEY.md and scripts/demo-no-key.sh | Reviewed; no Credentials-specific change because this demonstration explicitly excludes browser-console proof. Execution failed at Hermes discovery with `unsupported Hermes identity output`, before charter/config/design proof. Existing installed-console prose still claims bootstrap acquisition. |
| docs/RECORDING.md and docs/assets/aegis-no-key.{typescript,timing} | Reviewed and replayed successfully. Explicitly historical; current script/CLI cannot be inferred from replay. The recording requires regeneration with a supported runtime and review before publication. No recording was fabricated or overwritten. |
| .github/workflows/release.yml and scripts/verify-installed-mvi.sh | Reviewed build/checksum/publication boundary. Workflow runs the failing static console harness before packaging. Candidate verifier correctly denies the dirty tracked checkout. No exact changed-candidate release archives/checksums or installed-browser proof were produced; no GitHub release was created or claimed verified. |
| docs/contributing/ISSUE_BACKLOG.md | Reviewed as repository-local focused proposals, explicitly not remote GitHub issues. Credentials presentation does not change those proposals. Recording follow-up remains applicable; expertise-v2 reference in item 15 is pre-existing stale wording. No external issues were created. |

## Verified command results

- `go mod download`: passed.
- `go build -o aegis ./cmd/aegis`: passed with Go 1.26.6 available through `/home/silas/go/bin`.
- `python3 scripts/verify-console-vendor.py`: passed pinned Datastar digest.
- `python3 scripts/console_security_test.py`: FAILED on repeated runs with `navigation asset exceeds presentation-only authority: location.hash`.
- `go generate ./web/console`: passed; renderer SHA-256 unchanged before/after regeneration.
- `git diff --check`: passed; `gofmt -l cmd internal web` returned no files. A diff against HEAD is intentionally nonempty in this implementation worktree and is not generated drift.
- `go test ./web/console ./internal/api`: passed (cache eligible).
- `go test ./...`: passed (cache eligible).
- `go test -race ./...`: first run failed `TestPinentryProtocolSuccessEscapingAndStaticChrome` with `pinentry exited unsuccessfully after protected input`; focused race rerun with `-count=3` passed and subsequent full race run passed. The initial failure is retained as an intermittent observation, not hidden or attributed to this UI patch without evidence.
- `go vet ./...`: passed independently after the first race run short-circuited its chained invocation.
- `go run golang.org/x/vuln/cmd/govulncheck@v1.6.0 ./...`: passed, no called-symbol vulnerabilities; reported two imported-package and four required-module vulnerabilities not called by this code. Tool execution automatically selected Go 1.26.8; this is not a claim of a scan under Go 1.26.6.
- `./scripts/verify_installed_mvi_test.sh`: passed denial tests.
- `python3 -m unittest scripts/verify_release_archive_test.py scripts/verify_installed_fleet_vertical_test.py scripts/operator_acceptance_poc_test.py`: 10 tests passed.
- `./scripts/verify_release_candidate_test.sh`: passed named acceptance denial tests.
- `./scripts/verify-installed-mvi.sh`: exit 2, `release source provenance denied: tracked source worktree is dirty`. Gate was not bypassed and no commit was made during this review step.
- `AEGIS_BROWSER_PYTHON=/home/silas/.hermes/hermes-agent/venv/bin/python3 go test ./web/console -count=1 -v`: passed, including real Chrome at 1440/900/390px, independent panes/scroll, Back/Forward, explicit Back, filters/focus/selection, fragment routing and missing records. This is a synthetic Go-rendered metadata fixture, not an authenticated extracted-binary proof.
- `python3 -m unittest scripts.console_browser_test_test`: 23 tests passed.
- `go test ./cmd/aegis -run TestBasicAndAdvancedBootstrapRoutesReachSameArtifactDerivedStateAndResume -count=1`: passed.
- `./scripts/demo-no-key.sh`: exit 1 at unsupported Hermes discovery; the disposable workspace/executable cleanup is script-owned. No successful no-key end-to-end demonstration is claimed.
- `scriptreplay --timing docs/assets/aegis-no-key.timing docs/assets/aegis-no-key.typescript`: passed; historical replay only.

Interactive initialization, live model certification, provisioning/session launch, gateway install/start/stop/reset, update installation, release publication, and issue creation were not executed against the operator's environment. These are not safe unconditional documentation checks for this review. Exact candidate/release-readiness proof requires the later clean-source gate and an explicit candidate decision for the owner-decision workflow. Asciinema was not available on the inspected PATH. No full locally executable workflow completion is claimed where prerequisites failed.

## Findings retained for the sole loop gate

1. HIGH — launch/security harness contract mismatch. `scripts/console_security_test.py:57-66` forbids `location.hash` and pins a previous storage expression; `web/console/navigation.js` now deliberately reads fragments and separates detail storage. The tag-triggered release workflow invokes this harness, so otherwise passing Go/browser fixtures do not make the candidate publishable. Remediation must review the bounded navigation contract and add negative tests for hostile/malformed fragments, wrong origin/route, storage corruption, and native fallback; do not simply remove the security guard or relax CSP. Re-run the static harness and authenticated installed-browser proof.
2. HIGH — stale executable launch documentation. README and quickstart/demo prose still describe passwordless bootstrap while `internal/api/server_test.go` explicitly checks removed passwordless routes. Correct current prose and validate the actual password-authenticated path. Correct the architecture/backlog expertise-version drift against the implemented projection rather than treating historical prose as current.
3. HIGH — stale/unverifiable demo and recording. The installed Hermes discovery does not satisfy the demo, and the retained recording explicitly predates the current CLI. Obtain an approved supported runtime, rerun the real no-key script in isolation, regenerate and review recording bytes, replay, and retain real results without changing the normal Hermes profile or fabricating provider success.
4. GATE — clean-source packaging and installed proof remain outstanding. Preserve the dirty task-owned candidate, repair failures through the implementation loop, then use the later commit/exact-head verification stages to build and verify archives/checksums and installed console. Dirty-source denial is expected and is not itself a code defect.
5. OWNER GATES — private security-reporting availability and the private conduct-reporting destination require owner confirmation. No address, GitHub issue, release, or policy decision was invented. Do not consume implementation-loop attempts pretending those owner decisions can be repaired by code.

Judgment confirmed: real Chrome fixture behavior can pass while the repository release-security harness rejects the same candidate. Uncertain assumption: the intermittent pinentry race failure's cause; isolated and full reruns passed without changes. Missing evidence: supported real no-key runtime, regenerated recording, exact committed installed-candidate proof, and owner-designated reporting routes. Remediation pattern: document the precise presentation/authority distinction and retain executable gate failures rather than granting publication from layout tests.
