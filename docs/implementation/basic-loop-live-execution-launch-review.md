# Basic Loop execution: local launch impact review

Changed: README entry point, changelog, security policy, threat model, architecture path, quickstart cross-link, owning-service publication guide, executable hello walkthrough, Registry/Loop/Queue/router distributed guidance, operation matrix and canonical bundle digests.

Reviewed as unaffected: Apache LICENSE; CODE_OF_CONDUCT; CONTRIBUTING build/test requirements; no-key demo source and guide; terminal recording and recording guide; release script/checksum verifier; repository-local contributor issue backlog. The no-key demo/recording remain deterministic substrate evidence, not live Hermes/local-inference/hello proof. They were not regenerated or represented as new live acceptance. Release binaries/checksums were not published or regenerated; any release must rebuild from the final reviewed tree and verify its archive/checksums. No external issues were created.

Verification: hello CLI unit coverage checks canonical v2, exact fixed SHA-256 verifier policy, one action and required evidence, successor predecessors in both fields, invalid requests and no overwrite. Integrated command/API/app/core/registry/Hermes/local-inference/architecture/orchestration/Loop tests passed in this worktree. Initial skill test found a missing `control_plane_online` guidance token; guidance restored and bundle revalidated separately. Matrix and distribution validation are structural evidence, not runtime acceptance.

Limits/blockers: live Javi/model access unavailable. Synthetic session/attempt tests do not prove live acceptance. The runtime `message.complete` defect is repaired: success requires the installed protocol's explicit `status:"complete"` and valid text; errors and interruptions cannot retain successful output. Local model digest recheck is drift detection, not atomic pinning. Process-group/disposable-home custody is not a sandbox. Graph lifecycle application and general scheduling remain unsupported. No commits, pushes, installs, live operations, credentials or configuration changes were performed.

Online preparation follow-up: extended the explicit owning-service allowlist for charter source import/validation/exact readback, plan and approval operations (including rejection), root provision, and session preview/start/list/show/authority. Preserved separate operator approvals and kernel peer authentication. Updated the executable walkthrough, four preparation skills, exact hello wording, and obsolete runtime-binding matrix gap; regenerated the bundle with the product Go digest functions. Command and skillbundle suites and CLI build pass. Preparation payload tests use a synthetic Unix HTTP fixture; no live provisioning/session/provider acceptance is claimed. Other launch assets above were re-inspected and remain unaffected by this adapter-only preparation change. Parent retains full-suite and release verification.
Parent integration verification: independently reran `TestInstalledHelloOwningServiceChain` and the full `GOMAXPROCS=2 GOGC=30 go test -p 1 ./...` suite successfully. The installed CLI test uses real Unix peer authentication, application services and persistence; it preserves immutable predecessors, exercises explicit approval/provision/session, binds runtime authority, and independently verifies terminal hello evidence. Unapproved provisioning, workspace runtime binding, and an error completion carrying hello are rejected. Its gateway/model boundary is synthetic and is not evidence of execution on Javi's service. The skillbundle validator reports 15 valid skills, and `git diff --check` passes. Release artifact qualification and live operator acceptance remain unverified.
