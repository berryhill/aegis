# Execution admission launch-asset review

Follow-up verification: `TestInstalledOnlineLoopQueueSuccess` exercises the built CLI against the owning Unix peer-authenticated service and real stores, with synthetic gateway edits applied only by the real worker and independently executed native `TestHello`. Exact CLI readback and in-flight/terminal replay check one claim/attempt, evidence, disposition and preserved IDs. `TestAuthenticatedPreparationHTTPRoutes` authenticates through the HTTP login and covers empty, pending and cancelled collection/detail views; it is server-rendered HTTP proof, not real-browser navigation or live Javi/provider acceptance. The route test reproduced and fixed a historical-label defect: compatibility item state `queued` must not override the immutable initial preparation transition. Focused tests passed. A full `internal/api` run was attempted but timed out at 180 seconds; no full-suite pass is claimed here. Parent build/vet/full gates must rerun against `internal/api/console.go` after this fix.

This dirty-source patch adds native preparation collection/detail rendering, projection-based current state, historical initial-state labeling, and immutable-provenance/runtime-binding partitioning. README, execution-preparation/API specifications, Loop/Graph/Queue skills and the operation matrix were synchronized. Canonical skill manifest refresh and focused preparation/Loop execution/matrix/bundle tests passed locally.

Reviewed for this scope: LICENSE and CODE_OF_CONDUCT (unaffected legal/conduct terms); SECURITY and threat model (out-of-model authority, no foundational approval automation, no sandbox claim retained); CONTRIBUTING and repository-local ISSUE_BACKLOG (no publishing performed); ARCHITECTURE (separate preparation/binding/controller boundary retained); QUICKSTART and EXECUTABLE_HELLO (existing explicit lower-level path remains valid); DEMO_NO_KEY and RECORDING (explicitly exclude this workflow and retain historical/no-provider evidence, not regenerated).

Release scripts and checksum requirements were inspected, not executed. No release binaries, checksums, remote issues, deployment, live runtime, or live-provider acceptance were generated or verified for this dirty tree. Full quickstart/recording/release reproduction remains a release gate, not proof supplied by the focused tests. The create-only `queue-session-preparation` fence requires a matching create-only `queue-session-prepared` success receipt before reusing authority. Binding additionally requires the exact persisted running session and matching mandate with runtime session/process identity. An interrupted reservation cannot reuse active authority without that success proof; explicit operator recovery is required, not automatic crash reconciliation.
